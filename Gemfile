source "https://rubygems.org"

# Matches the Jekyll/theme/plugin versions GitHub Pages actually runs in production.
gem "github-pages", group: :jekyll_plugins

# Needed to run `jekyll serve` locally on Ruby 3+
gem "webrick", "~> 1.8"
